import { AppColors } from '../theme';

const AppConfig = {
  sceneProps: {
    hideNavBar: true,
    sceneStyle: {
      backgroundColor: AppColors.palette.white,
    },
  },
};

export default AppConfig;
